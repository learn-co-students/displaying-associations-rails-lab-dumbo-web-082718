<% @artists.each do |artist| %>
  <p><%= artist.name %> </p>
  <p>Song Count: <%= artist.song_count %> songs </p>
  <% artist.songs.each do |song| %>
    <ul>
      <li><%= link_to song.title, song_path(song) %></li>
    </ul>
  <% end %>
<% end %>